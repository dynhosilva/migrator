node_modules/
dist/
*.js.map
.env
!test/fixtures/**/.env
*.log

# Output de migração gerado em runtime — nunca versionar
/output/

# Workspace artifacts externos — projetos de exemplo locais (raiz apenas)
/strat-forge-pro/
/teste/
/fixtures/

# strat-forge-pro tem seu próprio .git — não tratar como submodule
examples/strat-forge-pro/

# Artefatos runtime do sync-users (backup, checkpoint, relatório HTML)
# Padrão: vão para os.tmpdir() — estas entradas protegem backupDir customizados
sync-backup-*.json
sync-backup-*.json.tmp
sync-checkpoint-*.json
sync-checkpoint-*.json.tmp
sync-report-*.html
lovable-migrate-sync/
